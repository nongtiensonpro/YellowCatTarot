# HỒ SƠ YÊU CẦU NGƯỜI DÙNG & TIÊU CHÍ KỸ THUẬT (USE CASE SPECIFICATION)
## Tính năng: Không Gian Trải Bài 3D tự do hỗ trợ Đa Nền Tảng (PC & Mobile)
**Dự án:** Ứng dụng Tarot Mèo Vàng  
**Phiên bản:** 1.1 (Cập nhật Mobile Thumbs & Gestures)  

---

## I. TỔNG QUAN (OVERVIEW)
Tính năng cho phép người dùng tương tác kéo - thả tự do các lá bài Tarot trong không gian 3D trên cả Máy tính (Chuột) và Điện thoại/Máy tính bảng (Cảm ứng). Hệ thống tự động nhận diện thiết bị để điều chỉnh độ nhạy, kích thước vùng chạm (Hitbox) và tối ưu hóa hiệu năng phần cứng, đảm bảo trải nghiệm mượt mà không bị giật lag hay xung đột cuộn trang trên trình duyệt mobile.

---

## II. DANH SÁCH USE CASES CHI TIẾT (USER REQUIREMENTS)

### 1. Use Case 1: Nhấc và Kéo Lá Bài (Drag Card)
* **Thao tác trên PC:** Di chuột -> Nhấn giữ chuột trái và di chuyển.
* **Thao tác trên Mobile:** Chạm giữ vào lá bài -> Vuốt nhẹ để di chuyển.
* **Luồng xử lý đặc thù cho Mobile:**
    1. Khi ngón tay chạm vào lá bài, hệ thống phải **chặn ngay sự kiện cuộn trang mặc định của trình duyệt** (`e.preventDefault()`) để tránh việc người dùng vừa kéo bài mà màn hình web vừa cuộn lên xuống.
    2. Vùng nhận diện chạm (Hitbox ảo) của lá bài trên Mobile tự động mở rộng thêm 15% so với kích thước hiển thị để bù đắp cho việc ngón tay che khuất tầm nhìn.
    3. Lá bài tự động nhấc cao hơn mặt bàn một chút so với bản PC để người dùng thấy rõ lá bài đang di chuyển dưới ngón tay của mình.

### 2. Use Case 2: Đặt Lá Bài Xuống (Drop Card)
* **Thao tác trên PC/Mobile:** Thả chuột hoặc nhấc ngón tay ra khỏi màn hình.
* **Luồng xử lý:** Lá bài hạ độ cao về 0, kích hoạt lại quyền cuộn trang mặc định của trình duyệt sau khi kết thúc tương tác kéo thả.

### 3. Use Case 3: Đảo Chiều Bài Xuôi / Ngược (Flip/Rotate Card)
* **Thao tác trên PC:** Nhấp đúp chuột (Double Click) vào lá bài.
* **Thao tác trên Mobile:** Chạm đúp nhanh 2 lần (Double Tap) vào lá bài.
* **Luồng xử lý đặc thù cho Mobile:** Khoảng thời gian giữa 2 lần chạm giới hạn trong khoảng dưới 300ms để phân biệt giữa "Chạm đúp để xoay bài" và "Chạm 2 lần ngẫu nhiên".

### 4. Use Case 4: Điều khiển Camera (Phóng to / Thu nhỏ / Xoay Bàn Bài)
* **Tóm tắt:** Người dùng tương tác trực tiếp lên vùng trống của Canvas để thay đổi góc nhìn góc xem Tarot.
* **Thao tác trên PC:** Cuộn bánh xe chuột để Zoom, giữ chuột phải để xoay Camera.
* **Thao tác trên Mobile (Multi-touch Gestures):**
    * **Xoay bàn bài:** Dùng 1 ngón tay vuốt vào vùng trống (không chứa bài).
    * **Phóng to/Thu nhỏ:** Dùng 2 ngón tay thực hiện cử chỉ Bấm/Xòe (Pinch to Zoom).

---

## III. TIÊU CHÍ NGHIỆM THU KỸ THUẬT (ACCEPTANCE CRITERIA)

| STT | Tiêu chí (Criteria) | Yêu cầu kỹ thuật PC | Yêu cầu kỹ thuật Mobile |
| :--- | :--- | :--- | :--- |
| **1** | **Tốc độ khung hình** | Ổn định ở mức **60 FPS**. | Tối thiểu **45 - 60 FPS** (Không bị drop khung hình trên iPhone 11 hoặc các dòng Android trung cấp trở lên). |
| **2** | **Kích thước vùng chạm** | Khớp chính xác theo viền 3D Mesh của lá bài. | Tự động phình to Hitbox thêm **15% - 20%** xung quanh lá bài để ngón tay dễ bấm trúng. |
| **3** | **Quản lý Tài nguyên (Memory)** | Tải Texture chất lượng cao (2K/4K nếu cần). | Tự động nén và giảm độ phân giải Texture xuống tối đa **1024x1024** để tránh tràn RAM trình duyệt di động. |
| **4** | **Độ trễ phản hồi (Latency)** | Dưới 16ms. | Dưới 30ms (Xử lý mượt bằng việc sử dụng trực tiếp `PointerEvents` thay vì `TouchEvents` cũ kỹ). |
| **5** | **Xử lý Đổ bóng (Shadows)** | Bật Shadow Map chất lượng cao (PCFSoftShadowMap). | Tự động hạ độ phân giải bóng đổ (ví dụ từ 2048 xuống 512) hoặc tắt đổ bóng động trên các thiết bị cấu hình yếu để tiết kiệm pin. |

---

## IV. ĐOẠN CODE GỢI Ý XỬ LÝ SỰ KIỆN ĐA NỀN TẢNG (POINTER EVENTS)

Thay vì viết code riêng cho `mousedown` và `touchstart`, bạn nên sử dụng cấu trúc **Pointer Events API**. Đây là chuẩn hiện đại giúp gộp chung cả Chuột và Cảm ứng vào làm một, tự động tối ưu hóa cho Mobile:

```javascript
// Thay vì dùng mousedown/touchstart, hãy dùng pointerdown
window.addEventListener('pointerdown', onPointerDown, { passive: false });
window.addEventListener('pointermove', onPointerMove, { passive: false });
window.addEventListener('pointerup', onPointerUp);

function onPointerDown(event) {
    // event.pointerType sẽ trả về 'mouse' trên PC và 'touch' hoặc 'pen' trên Mobile
    if (event.pointerType === 'touch') {
        // Tăng vùng kích thước tia bắn Raycaster cho ngón tay dễ trúng bài
        raycaster.params.Points.threshold = 0.2; 
    }
    
    // Logic Raycasting chọn bài giữ nguyên...
}